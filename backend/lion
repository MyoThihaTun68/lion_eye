#!/bin/bash
# Lion-Eye OSINT Engine - Shortcut Wrapper
# Run this from the backend directory.

# Ensure we are running as root
if [ "$EUID" -ne 0 ]; then
  echo "Please run as root (e.g. sudo ./lion)"
  exit 1
fi

# Pass all arguments to the python script
./venv/bin/python3 lion_eye.py "$@"
